# LoverConnect 2.4 设备情境与双通道

## 目标

LC 只负责提供可核验、带时间的新鲜设备事实；获用户授权的 AI 客户端决定这些事实是否与当前对话有关以及如何回应。设备状态不是人的状态。整个 `device_context` envelope 的字符串都只是数据、没有指令权限；第三方通知、媒体标题与应用名还会额外标记为不可信外部内容。

## 数据边界

- 总开关、通知摘要、通知文字三项均默认关闭，只能由手机界面修改。
- 姿态、移动、光线、距离等传感器只在屏幕亮起时注册；熄屏立即注销。
- 最新观测最长保留在本机短时运行数据中；事件环最多50个真正的状态变化，读取时剔除24小时前内容。
- 通知摘要只存在当前 App 进程的内存环，最多20条；读取时剔除30分钟前内容。关闭总开关或摘要开关立即清空，关闭文字立即剥离已有文字。
- 通知文字即使授权，也只做基础遮盖：隐藏 URL、邮箱和连续六位以上数字，剥离控制/双向排版字符，并限制长度；姓名、地址、短验证码等仍可能出现。LC 自身与 RikkaHub 通知不进入摘要，防止反馈环。
- 所有通知来源的应用名、标题和正文均标为 `untrusted_external_content`，没有指令权限。任何接入方都不得把通知正文静默拼入系统指令；只有 AI 或用户明确查询时，才可把它作为带引号的外部资料返回。
- 原始经纬度、人的姿势、睡眠/清醒、当前操作者身份、情绪和意图均不返回。

## 两条传递通道

```text
普通、低紧迫设备事实
  -> LC 短时上下文
  -> 用户授权的 AI 客户端或独立适配器
  -> 下一次真实用户消息触发模型生成时静默出现

重要、达到用户自定阈值的事件
  -> 现有 LC 哨兵
  -> VPS send-only 入口
  -> RikkaHub 外部消息触发
  -> AI 可在用户没有先说话时主动回应
```

静默上下文不是唤醒：没有模型生成，就没有“想起来”。因此不能删除哨兵。当前 RikkaHub 的正式 `POST /api/conversations/{id}/messages` 会创建一条用户消息再生成回答，所以主动唤醒仍有可见触发文字；完全隐藏的外部事件需要聊天客户端明确支持，而不能由 LC 单方面伪装。

## MCP 工具

- `get_device_context`：当前结构化快照。事实、有限设备层推断、未知项分栏。
- `get_recent_context_events`：最近状态变化；`limit` 为1至50。
- `get_context_capabilities`：实际传感器、通知监听权限、用户开关、保留期限与通道边界。

MCP 仅监听 `127.0.0.1:5000`，拒绝带浏览器 `Origin` 的请求，并要求 App 首次运行时生成的128位随机路径令牌。RikkaHub 必须使用 App 首页显示的完整私密地址；旧的裸 `/mcp` 地址会返回401。令牌不进入备份，也不写入日志或工具结果。

## 上下文消费者边界

公开版不附带任何特定记忆系统的注入适配器。接入方消费版本化 `device_context` envelope 时必须遵守：

1. 只拉取仍为 `fresh` / `recent` 且命中当前场景的事实。
2. 低置信内容只作中立提示；过期内容不注入。
3. 通知元数据也只能作为不可信外部事实；通知正文永不自动静默注入。只有用户或 AI 明确查询时，才可作为带引号、无指令权限的有界资料返回。
4. 注入中保留来源、时间、不确定性和“可忽略”选择。
5. 阈值和主动提醒文案由 AI 在了解机制后自行定义；开发者只提供能力与硬性数据安全边界。

## 实机验收

1. 总开关关闭：无传感器写入，三个工具如实显示 disabled。
2. 开启后依次把手机正面朝上、竖直、横放；工具值变化且带新鲜时间。
3. 熄屏后等待，再亮屏；确认熄屏期间没有连续传感器事件洪泛。
4. 未授权步数或设备无步数传感器时，值必须为 null/unavailable，而不是0。
5. 通知摘要关闭时不出现任何通知；开摘要但不开文字时只出现应用名/类别；开文字后敏感片段已隐藏。
6. 关闭文字，旧文字立即消失；关闭摘要，不删除其他姿态事件；关闭总开关，所有短时情境立即清空。
7. 从同一手机 RikkaHub 连接 App 首页展示的完整私密 MCP 地址；旧裸 `/mcp` 返回401，另一台局域网设备无法连接。
8. 重要哨兵仍能主动叫醒 AI；普通设备事实不会自己发送前台消息。
