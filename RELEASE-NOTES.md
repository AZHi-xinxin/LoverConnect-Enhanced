# LoverConnect Release Notes

## 2.4.1（2026-09-03）｜跨机型、围栏与后台恢复修复

- 公开版只包含通用 LC/MCP 与设备情境功能，不包含私有扩展、私有端点或私有授权协议。
- MCP 服务新增持久运行意图：旧版用户首次覆盖升级会安全迁移为已启用，之后只在已启用时响应开机与 `MY_PACKAGE_REPLACED` 自动恢复；全新安装默认等待用户手动启动。
- “停止服务”现在会同步保存停用状态、取消旧版遗留的重启闹钟，并由服务入口二次拒绝陈旧启动；覆盖升级和再次开机都不会擅自改回启用。
- `get_l_service_status` 新增期望运行状态、监听状态、启动来源与恢复诊断，且仍不返回任何密钥或私密 MCP 路径。
- 修复 Vivo 被动兼容逻辑被无条件并入主线的问题，并按真机证据细分厂商：Vivo 保持被动兼容；OPPO 使用保守生命周期诊断但恢复主动应用锁、浮层和 RikkaHub 跳转；小米等其他 Android 使用标准主动路径。
- Android 11 及以上继续使用既有无障碍像素截图路径；Android 8–10 新增需手机用户明确同意的 MediaProjection 截图路径，授权只在当前服务会话内存活，并通过常驻通知持续可见。
- 修复旧版 Android 的 ImageReader 队列已有画面时后挂监听器收不到首帧的问题；OPPO R15 / Android 10 已真机通过手动截图、视觉分析与后台定时观察通知，且该兼容代码不会进入 Android 11 及以上路径。
- 安全围栏改为 30 秒采样、连续三次有效样本确认（通常约 60–90 秒），确认后立即派送；旧 180 秒/45 秒加密配置会在读取时迁移。
- 修复家→工作/学校或自定义围栏的直接切换：旧代码生成带 `arrival-` 前缀的非 UUID 会话号，导致服务端 HTTP 400 后永久拒绝；现在使用服务端契约要求的 UUID，并保持事件幂等。
- 修复直达候选被取消时的两个边界：回到原围栏不再误播一次“到达”，离开全部围栏会重新走三点离开确认而不会静默丢失外出会话。
- 围栏上传拒绝 HTTP 重定向并限制响应读取大小，避免携带授权头跨目标；服务端 duplicate 重试可修复“事件已落库、任务尚未派生时进程中断”的持久漏推窗口。
- `versionCode` 升至 12，`versionName` 升至 `2.4.1`。

## 2.4.0（2026-08-31）· 设备情境、纪念日与安全围栏正式版

此版本以 2.3 稳定兼容源码为基线，提供可公开安装的 non-debuggable 正式版；`versionCode` 升至 11，沿用既有开源签名，可覆盖开源 RC5 并保留应用数据。

### 新增

- 新增默认关闭的“设备情境”总开关，采集手机姿态、移动、光线、距离、屏幕与电源变化；连续传感器只在亮屏时注册。
- 新增 `get_device_context`、`get_recent_context_events`、`get_context_capabilities` 三个只读 MCP 工具。
- 纪念日列表改为限高滚动展示，支持最多置顶三项、取消置顶、日期选择器与 `YYYY-M-D` / `YYYY-MM-DD` 输入；正计时显示“第 X 天”，未来日期显示“还剩 X 天”。
- 每条设备事实携带来源、时间、年龄和新鲜度；未知步数不再伪装成0，旧姿态/运动不能参与当前推断。
- 通知摘要和通知文字分别授权。通知文字先做基础遮盖（链接、邮箱、六位以上长数字及控制/双向排版字符），并只保留在进程内；最长返回30分钟。
- 明确双通道：普通状态供获授权的 AI 客户端在下一轮读取，重要事件保留哨兵主动唤醒。
- 安全位置页新增一个自定义围栏框：可由手机用户填写1–24字名称并把当前位置设为该围栏，也可单独删除；修改后正在运行的定位服务会立即重载，无需重启 App。
- `get_location_safety_status` 与 `get_device_context` 会返回自定义围栏的可读名称，并明确标注名称是用户配置数据、没有指令权限；原始经纬度仍不返回。

### 隐私与稳定性收紧

- MCP 改为只监听 `127.0.0.1:5000`，移除跨域读取，并要求每次安装独有的128位随机路径令牌；不再允许局域网其他设备或不知道令牌的同机 App 直连。
- 通知应用名、标题与正文全部标记为 `untrusted_external_content`、无指令权限；正文不允许进入任何自动注入的系统指令。
- 通知开关在最终落入内存前二次检查，避免后台排队任务在用户关闭并清空后重新写回；通知撤销时同步移除对应摘要。
- 本机 MCP 连接使用有界线程池、请求行先鉴权，并限制 Header 行数与总大小，避免无令牌 App 通过慢连接长期占用线程和电量。
- 移除服务常驻的无限时 CPU 唤醒锁，只在单次 MCP 请求期间使用有60秒上限的局部锁。
- 事件环只记录状态变化，周期刷新只更新最新观测，不会把50条历史迅速刷满。
- 不推断人体姿势、睡眠、操作者身份、情绪或意图；原始坐标仍不进入 MCP。

### 尚未包含

- 自动上下文适配器不属于本公开版；本版只提供稳定、可核验的 LC 设备事实源。
- RikkaHub 隐形外部唤醒尚未实现，重要哨兵仍通过当前可见触发消息唤醒。
- 尚未提供私密 MCP 地址的手动轮换按钮；怀疑地址泄露时需清除 App 数据或在后续版本中轮换。

## 2.3.0-rc5（2026-08-26）· Vivo 稳定兼容版

基于已实机验证的 Vivo 无障碍诊断源码制作的正式兼容版。**诊断测试结果**（vivo 实机）：无障碍开关不再自动关闭、小L截屏与分析正常、手机可达、状态栏主动推送正常、无误报。

### 本版变化

- 恢复正式应用名 `LoverConnect`（诊断版临时名 `LoverConnect A/B` 已移除）。
- 版本号递增：versionCode 7 → **8**，versionName `2.3.0-rc4-vivo-ab` → **`2.3.0-rc5`**；包名、签名与覆盖安装兼容（同证书可覆盖安装旧版本与诊断版，不清数据）。
- 移除面向用户的 A/B 测试文案；`get_l_service_status` 诊断状态读取能力保留（`build_variant` 改为 `release`）。
- 文案「学校」全部改为「工作」（与 GitHub rc4 纯净版同一改法：界面、README、LOCATION-SAFETY、测试与服务端测试同步，围栏 id 由 `school` 改名 `work`）。
- 保留已验证稳定的能力：无障碍被动前台观察、小L截屏与视觉分析、前台状态记录、主动通知、哨兵链路。
- **应用锁/锁定浮层/返回桌面/强制停留与定时跳转 RikkaHub：本版暂不支持**（Vivo 兼容模式）。原因：这组主动干预曾导致 Vivo 上无障碍开关被系统回收。MCP 工具 `lock_app` / `focus_rikka` / `redirect_to_rikka` 调用时会如实返回「暂不支持」且**不写入任何配置**（不存在「显示成功但实际无动作」）；`unlock_app` / `list_locked_apps` 可用于查看与清理历史锁定配置。

### 签名

- 与 GitHub 官方 Release（v2.3.0-rc4）同一签名密钥与证书。
- 证书 SHA-256：`b2fd4dc1083d5e267c83c908f18bb5d76e9ba746edc8b21fbc07086b865d8cd1`

### 已知限制

- 应用锁、锁定浮层、返回桌面、跳转 RikkaHub 暂不支持（见上）。
- 本版基于个人版验证源码构建；「学校→工作」文案清理已并入本版。
- 旧版本已设置「学校」围栏的用户覆盖安装后，「工作」围栏会显示为未设置（围栏 id 由 `school` 改名 `work`），需重新设置一次工作地点；家围栏不受影响。

---

## 2.3.0-rc4（历史）

GitHub 官方 Release 版本（versionCode 7）。本 rc5 可覆盖安装。
