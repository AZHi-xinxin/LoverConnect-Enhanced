# LoverConnect 2.4.3 · 观察返回解析与诊断（源码更新）

**本次只发布公版源码，不附 APK。** 原公版签名文件尚待恢复，不能用调试签名或私人签名替换，以免已安装用户无法保留数据覆盖升级。此前 Releases 的旧版 APK 不包含本次改动。不要通过卸载 App 解决签名不一致。

版本：`2.4.3`，`versionCode 15`。

## 更新内容

- 从视觉模型返回中提取有效自然语言正文，兼容完整思考段后的最终 JSON、纯文本回复及文本分段；拒绝多个歧义对象、非字符串 message、混合非文本内容和损坏结构，不将内部数组/对象序列化后直接当日记。
- 区分空正文、截断、拒答、格式错误和写入失败。`get_l_service_status` 新增版本号与 `eyes_analysis_last`，方便反馈时定位哪一步没有完成。
- 诊断仅包含固定原因码、时间、类型/长度和数值用量，不包含截图、提示词、模型正文、API 地址或密钥；正常日记正文仍按原有功能保存在手机。
- 手动观察超过 30 秒尚未完成时提示“仍在进行”，不把等待超时直接称为截屏失败，也不自动重发。
- 写入串行化，整理采用原子文件写入；写入成功但提醒失败单独报告。历史日记不自动删除或重写。
- 累积包含 2.4.2 的观察日记空条目防护、围栏半径独立二次保存；不改围栏中心点，不增加权限，不包含私人授权签发功能。

## 验证范围

- 公版 Debug / Release 各 **105 项单元测试通过**，编译及 lint 检查通过。
- 同一补丁在手机上的手动观察、新增日记及后续事件链路已由用户验收。
- 公版签名 APK 尚未产出，不能称该 APK 已真机验收；本版自然定时观察仍待独立确认。
- 最初空结果没有充分原始响应诊断，无法确定唯一根因。本版修复已复现的解析/写入边界并补诊断，不宣称所有供应商、模型输出和设备场景均已解决。

## 更新与反馈

源码可通过此 tag 下载或拉取。自行构建请使用自己的既有签名，签名配置见 [RELEASE-SIGNING.md](RELEASE-SIGNING.md)；它不保证能覆盖安装官方旧包。已有用户请保留现有安装和数据，等待同公版证书签名的安装包。

如观察异常，串行读取日记 → 手动观察 → 完成后再读日记及服务状态。反馈 App 版本、是否有新增正文和最近一次固定诊断码即可；不要提交密钥、私密 MCP 地址、截图或私人日记正文。旧历史 JSON 条目仍可存在，不能单凭旧条目认定新版本复发。
