# LoverConnect Enhanced 2.4.1 公开版差异

本目录承接 LoverConnect Enhanced 2.4.0 公开版，并加入跨机型应用拦截、安全围栏链路及 MCP 服务升级恢复修复。公开源码不包含任何私有记忆系统的签发界面、令牌配置或协议实现。

## 2.4.1 新增与调整

- MCP 服务记住用户的启用/停用意图；旧版首次覆盖升级会迁移为启用，之后只在已启用时响应开机与覆盖更新恢复，全新安装默认手动启动。
- 移除旧版无条件 3/5 秒闹钟自救；用户点击停止后，陈旧启动请求会在服务入口再次被拒绝。
- `get_l_service_status` 增加服务期望状态、端口监听状态、启动来源与恢复诊断，但不返回私密路径或密钥。
- 同一 APK 按 Android `manufacturer` / `brand` 在运行时分流：Vivo 使用 `OEM_CONSERVATIVE + OEM_PASSIVE_COMPAT`；OPPO 使用 `OEM_CONSERVATIVE + ACTIVE`；小米及其他 Android 使用标准主动路径。AI 无法更改该判断。
- 小L像素观察按系统版本分流：Android 11 及以上沿用无障碍截屏；Android 8–10 仅在手机用户明确同意系统 MediaProjection 弹窗后启用，授权只存在于当前服务会话并持续显示通知。
- 安全围栏改为约每 30 秒采样、连续 3 次有效样本后立即排队发送；旧版位置配置会保留围栏并迁移到当前固定确认周期。
- 修复从一个围栏直接进入学校、工作地点或自定义围栏时使用了非 UUID 会话号，导致服务端 HTTP 400 且通知永久漏发的问题；新事件使用契约要求的 UUID 并维持幂等去重。
- 配套 ingress 源码在手机已经完成三次确认后不再额外等待；部署端仍保留持久队列、幂等和失败重试。
- 正式版本为 `versionCode 12` / `versionName 2.4.1`。

## 不进入公开仓库的内容

- 签名密钥库、口令、令牌、真实服务端环境文件与本机路径；
- 手机专用安装包、设备配置、运行数据库、日志、备份与截图；
- 开发过程的内部技术报告、设计草稿、本机 `local.properties`、构建缓存和构建产物。

公开 APK 继续使用 LoverConnect Enhanced 既有的开源发布证书，可覆盖同一公开签名链上的 2.4.0 与更早版本。私人测试签名与公开签名互不兼容。
