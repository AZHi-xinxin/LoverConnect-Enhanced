# LoverConnect Enhanced 2.4.0 公开版差异

本目录以 `AZHi-xinxin/LoverConnect-Enhanced` 的 MIT 主分支快照
`010308b8ec3f8890f6ad4ccfc1ff557b2b9c2761` 为公开基线，并同步了上游纪念日界面的后续改进。

## 2.4.0 新增与调整

- 新增默认关闭、由手机用户自行授权的设备情境采集，以及
  `get_device_context`、`get_recent_context_events`、`get_context_capabilities` 三个只读 MCP 工具。
- 新增自愿安全位置播报与一个可自定义名称的围栏；坐标在手机本地加密保存，MCP 只返回粗粒度状态和用户配置的区域名称。
- 本机 MCP 仅监听 `127.0.0.1`，使用每次安装独有的随机路径令牌，并拒绝浏览器 Origin。
- 通知内容被标记为不可信外部数据；通知文字单独授权、基础遮盖、仅短时驻留内存。
- 公网视觉 API 必须使用 HTTPS；HTTP 仅允许同设备回环地址。
- Android 备份规则排除配置、诊断、记忆、观察日志和设备情境运行态，避免敏感配置进入云备份或换机备份。
- 纪念日支持限高滚动、最多三项置顶、日期选择器、宽松日期输入，以及正计时/倒计时展示。
- 正式版本为 `versionCode 11` / `versionName 2.4.0`。

## 不进入公开仓库的内容

- 私人签名库、口令、令牌、真实服务端环境文件与本机路径；
- 手机专用安装包、设备配置、运行数据库、日志、备份与截图；
- 开发过程中的内部技术报告、设计草稿、构建缓存和构建产物。

公开 APK 使用 LoverConnect Enhanced 既有的开源发布证书签名，可覆盖同一公开签名链上的旧版本。私人测试签名与公开签名互不兼容。
